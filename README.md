# Order-n-Bill-Management-System
## Set up project
to <strong>install</strong> required libs: <code>pip install -r requirements.txt </code> \
to <strong>run</strong> the project: <code>py app.py</code>
